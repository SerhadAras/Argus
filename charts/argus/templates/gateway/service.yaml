---
apiVersion: v1
kind: Service
metadata:
  name: "gateway-server"
  labels:
    {{- include "argus.labels" . | nindent 4 }}
spec:
  selector:
    app: "gateway"
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
