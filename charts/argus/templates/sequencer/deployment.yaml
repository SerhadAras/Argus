---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "sequencer"
  labels:
    {{- include "argus.labels" . | nindent 4 }}
    app: "sequencer"
  annotations:
    {{- toYaml .Values.sequencer.annotations | nindent 4 }}
spec:
  replicas: {{ .Values.sequencer.replicas.min }}
  selector:
    matchLabels:
      app: "sequencer"
  template:
    metadata:
      annotations:
        {{- set .Values.sequencer.podAnnotations "autocert.step.sm/name" "sequencer-server" | toYaml | nindent 8 }}
      labels:
        app: "sequencer"
    spec:
      tolerations:
        {{- toYaml .Values.sequencer.tolerations | nindent 8 }}
      affinity:
        {{- toYaml .Values.sequencer.affinity | nindent 8 }}
      nodeSelector:
        {{- toYaml .Values.sequencer.nodeSelector | nindent 8 }}
      imagePullSecrets:
        {{- toYaml .Values.image.imagePullSecrets | nindent 8 }}
      securityContext:
        {{- toYaml .Values.sequencer.securityContext | nindent 8 }}
      containers:
        - name: "sequencer"
          image: "{{ .Values.image.registry }}/sequencer:{{ .Values.sequencer.tag }}"
          imagePullPolicy: "{{ .Values.image.pullPolicy }}"
          ports:
            - containerPort: 3000
          resources:
            requests:
              memory: "15Mi"
              cpu: "100m"
            limits:
              memory: "50Mi"
              cpu: "300m"
          env:
            # Web port
            - name: PORT
              value: "3000"

            # Redis settings
            - name: REDIS_HOST
              value: "{{ .Values.redis.fullnameOverride | default (printf "%s-redis" .Chart.Name ) }}"
            - name: REDIS_PORT
              value: "26379"
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: "{{ .Values.redis.fullnameOverride | default (printf "%s-redis" .Chart.Name ) }}"
                  key: redis-password

            - name: LOGLEVEL
              value: "{{ .Values.sequencer.loglevel }}"

            {{- if .Values.security.cluster.tls}}

            - name: TLS_ENABLED
              value: "TRUE"
            - name: CERT_PATH
              value: "/var/run/autocert.step.sm/site.crt"
            - name: KEY_PATH
              value: "/var/run/autocert.step.sm/site.key"
            - name: CA_PATH
              value: "/var/run/autocert.step.sm/root.crt"

            {{- else }}

            - name: TLS_ENABLED
              value: "FALSE"

            {{- end}}
